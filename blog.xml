<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Nuhaadh Mahid</title>
<link>https://nuhaadhmahid.com/blog.html</link>
<atom:link href="https://nuhaadhmahid.com/blog.xml" rel="self" type="application/rss+xml"/>
<description>Personal Website of Nuhaadh Mahid</description>
<generator>quarto-1.8.25</generator>
<lastBuildDate>Thu, 25 Dec 2025 18:42:17 GMT</lastBuildDate>
<item>
  <title>C++ Crash Course</title>
  <link>https://nuhaadhmahid.com/blog/Cpp_Crash_Course/</link>
  <description><![CDATA[ 




<p>In this blog post we will go through the basic concepts of C++ programming. I will try to keep these notes brief, and guide the reader to the appropriate resources for further details. In doing so, We hope these notes will serve as a refresher for me and the readers.</p>
<p>I am using Jupyter notebook to write this blog post, as it allows me to keep the text, code and commands in one place, and compile it to this HTML page using <a href="https://quarto.org/">Quarto</a>. I am using the following IPython magic commands to write files and execute shell commands:</p>
<ul>
<li><code>%%writefile &lt;path-to-file&gt;.cpp</code></li>
<li><code>%%sh</code></li>
</ul>
<p>I am executing the commands in a Linux environment, and these commands may be different on a Windows environment. I am using Windows Subsystem for Linux (WSL)<sup>1</sup> on my Windows machine.</p>
<section id="hello-world" class="level2">
<h2 class="anchored" data-anchor-id="hello-world">Hello, World!</h2>
<p>As with any programming language, we start by printing <code>Hello, World!</code> to the command-line interface (CLI), using the code below:</p>
<div id="a3627fb9" class="cell" data-execution_count="1">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode numberSource python number-lines code-with-copy"><code class="sourceCode python"><span id="cb1-1"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%%</span>writefile source<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello.cpp </span>
<span id="cb1-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#include &lt;iostream&gt;</span></span>
<span id="cb1-3"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">int</span> main() {</span>
<span id="cb1-4">    std::cout <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&lt;&lt;</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"Hello, World!"</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&lt;&lt;</span> std::endl<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span></span>
<span id="cb1-5">    <span class="cf" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">return</span> <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">;</span></span>
<span id="cb1-6">}</span></code></pre></div></div>
<div class="cell-output cell-output-stdout">
<pre><code>Writing source/hello.cpp</code></pre>
</div>
</div>
<p>In this blog post, we will introduce different concepts as we go, alongside the example code. We start by discussing the code above, line by line:</p>
<ul>
<li><strong>Line 1</strong>: We create a file with the extension “.cpp”. You might also come across file with the extension “.cc”. However, throughout this blog we will be using the former.</li>
<li><strong>Line 2</strong>: We import the header <code>&lt;iostream&gt;</code><sup>2</sup> from the standard template library (STL). It gives us the definition of <code>std::cout</code> which we use print to the CLI, and <code>std::endl</code> which we use to print a newline character. <code>std</code> here refers to a namespace, which is a container that collects names of variables, classes, functions, etc, that are defined within it.</li>
<li><strong>Line 3</strong>: We define a function called <code>main</code> with the return type <code>int</code> which stands for integer. We have to declare the type of the variable, which makes C++ a statically typed language. This <code>main</code> function is where the program starts to execute. The curved brackets <code>()</code> after <code>main</code> contains the list of parameters (in this case none), which we pass into the function. Next is the curly brackets <code>{}</code> which contains all the statements of the function.</li>
<li><strong>Line 4</strong>: We pass the string <code>Hello, World!</code> followed by a newline character using <code>std:endl</code> to the object <code>std:cout</code>, which then prints out the contents passed into it to the CLI. The statement ends with a semi-colon <code>;</code>, which is case for all statements in C++ syntax.</li>
<li><strong>Line 5</strong>: We returns the integer <code>0</code> from the function. <code>return</code> is a keyword in C++ which is reserved by the language.</li>
</ul>
<p>Before we could execute the above code, we will need to compile it to a program. This makes C++ a compiled language. Here we compile the code and the execute the program, as shown below:</p>
<div id="5b24b881" class="cell" data-execution_count="2">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode numberSource python number-lines code-with-copy"><code class="sourceCode python"><span id="cb3-1"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%%</span>sh</span>
<span id="cb3-2">g<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">++</span> source<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello.cpp <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>o build<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello </span>
<span id="cb3-3">.<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>build<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello</span>
<span id="cb3-4">echo $?</span></code></pre></div></div>
<div class="cell-output cell-output-stdout">
<pre><code>Hello, World!
0</code></pre>
</div>
</div>
<p>Again, discussing line by line:</p>
<ul>
<li><strong>Line 2</strong>: We ask the <code>g++</code> to take the file <code>source/hello.cpp</code> and output <code>build/hello</code> after the full compilation process.</li>
<li><strong>Line 3</strong>: We execute the program, and it prints to the CLI “Hello, World!”.</li>
<li><strong>Line 4</strong>: We check the return code of the program, which in this case is integer <code>0</code> as we defined in our program.</li>
</ul>
</section>
<section id="compilation-process" class="level2">
<h2 class="anchored" data-anchor-id="compilation-process">Compilation Process</h2>
<p>The compilation process has multiple steps. These include:</p>
<ol type="1">
<li><code>Pre-processing</code>: handles lines that start with <code>#</code>, such as <code>#include</code> and <code>#define</code>.</li>
<li><code>Compiling</code>: translates the pre-processed code to assembly code.</li>
<li><code>Assembling</code>: translates the assembly code to machine code, producing object files.</li>
<li><code>Linking</code>: links the object files and libraries (e.g., <code>&lt;iostream&gt;</code>) to produce the final executable program.</li>
</ol>
<p>In the pre-processing stage, the compiler finds the headers we have defined and replace the include statement (e.g., <code>#include &lt;iostream&gt;</code>) with the contents of that header file. We can view the result of this process by stopping the compilation process after the preprocessing stage, as show below:</p>
<div id="1a0456bf" class="cell" data-execution_count="3">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode numberSource python number-lines code-with-copy"><code class="sourceCode python"><span id="cb5-1"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%%</span>sh</span>
<span id="cb5-2">g<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">++</span> source<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello.cpp <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>o build<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello.ii <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>E </span>
<span id="cb5-3">head <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>n <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">20</span> build<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello.ii                   </span></code></pre></div></div>
<div class="cell-output cell-output-stdout">
<pre><code># 0 "source/hello.cpp"
# 0 "&lt;built-in&gt;"
# 0 "&lt;command-line&gt;"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "&lt;command-line&gt;" 2
# 1 "source/hello.cpp"
# 1 "/usr/include/c++/13/iostream" 1 3
# 36 "/usr/include/c++/13/iostream" 3
       
# 37 "/usr/include/c++/13/iostream" 3

# 1 "/usr/include/c++/13/bits/requires_hosted.h" 1 3
# 31 "/usr/include/c++/13/bits/requires_hosted.h" 3
# 1 "/usr/include/x86_64-linux-gnu/c++/13/bits/c++config.h" 1 3
# 306 "/usr/include/x86_64-linux-gnu/c++/13/bits/c++config.h" 3

# 306 "/usr/include/x86_64-linux-gnu/c++/13/bits/c++config.h" 3
namespace std
{
  typedef long unsigned int size_t;</code></pre>
</div>
</div>
<p>In the shell command above, we am using the compiler <code>g++</code> which belong to the GNU compiler collection (GCC)<sup>3</sup>. Let’s break done the shell commands, line by line:</p>
<ul>
<li><strong>Line 2</strong>: We ask <code>g++</code> to take the file <code>source/hello.cpp</code> and output <code>build/hello.ii</code> after the preprocessing stage. The flag for asking <code>g++</code> to output a file is <code>-o</code> and the flag for asking <code>g++</code> to stop after preprocessing stage is <code>-E</code>. You can view a list of available flag options using the command <code>g++ --help</code>.</li>
<li><strong>Line 3</strong>: We are displaying only the first 20 lines of the output file, as the file is very long. I recommend, having a look at the file on GitHub repository. It shows that the <code>include</code> statement was replaced with the contents of the header file. The third last line printed above shows the definition of namespace <code>std</code>. Every name defined within the curly brackets <code>{}</code> after it belongs to the namespace <code>std</code>.</li>
</ul>
<p>Similarly, can also check the assembly code after the compiling stage. The assembly code is specific to the architecture of the machine it is being compiled on. In my case, it is x86-64 architecture. Here we ask the compiler to stop after the compiling stage, as shown below:</p>
<div id="353a2898" class="cell" data-execution_count="4">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode numberSource python number-lines code-with-copy"><code class="sourceCode python"><span id="cb7-1"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%%</span>sh</span>
<span id="cb7-2">g<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">++</span> source<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello.cpp <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>o build<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello.asm <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>S </span>
<span id="cb7-3">head <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>n <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">20</span> build<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello.asm</span></code></pre></div></div>
<div class="cell-output cell-output-stdout">
<pre><code>    .file   "hello.cpp"
    .text
#APP
    .globl _ZSt21ios_base_library_initv
    .section    .rodata
.LC0:
    .string "Hello, World!"
#NO_APP
    .text
    .globl  main
    .type   main, @function
main:
.LFB1988:
    .cfi_startproc
    endbr64
    pushq   %rbp
    .cfi_def_cfa_offset 16
    .cfi_offset 6, -16
    movq    %rsp, %rbp
    .cfi_def_cfa_register 6</code></pre>
</div>
</div>
<p>Again, we are showing the first 20 lines of the output file here. The rest of the file can be found on the GitHub repository.</p>
<ul>
<li><strong>Line 2</strong>: We ask <code>g++</code> to take the file <code>source/hello.cpp</code> and output <code>build/hello.asm</code> after the compiling stage. The flag for asking <code>g++</code> to stop after the compiling stage is <code>-S</code>.</li>
</ul>
<p>If we have only the executable file and not the source file, we can still view the assembly code by disassembling the executable file, as shown below:</p>
<div id="eb937177" class="cell" data-execution_count="5">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode numberSource python number-lines code-with-copy"><code class="sourceCode python"><span id="cb9-1"><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">%%</span>sh</span>
<span id="cb9-2">objdump <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>d <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>M intel build<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> build<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello_2.asm</span>
<span id="cb9-3">head <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>n <span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">20</span> build<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>hello_2.asm</span></code></pre></div></div>
<div class="cell-output cell-output-stdout">
<pre><code>
build/hello:     file format elf64-x86-64


Disassembly of section .init:

0000000000001000 &lt;_init&gt;:
    1000:   f3 0f 1e fa             endbr64
    1004:   48 83 ec 08             sub    rsp,0x8
    1008:   48 8b 05 e1 2f 00 00    mov    rax,QWORD PTR [rip+0x2fe1]        # 3ff0 &lt;__gmon_start__@Base&gt;
    100f:   48 85 c0                test   rax,rax
    1012:   74 02                   je     1016 &lt;_init+0x16&gt;
    1014:   ff d0                   call   rax
    1016:   48 83 c4 08             add    rsp,0x8
    101a:   c3                      ret

Disassembly of section .plt:

0000000000001020 &lt;.plt&gt;:
    1020:   ff 35 8a 2f 00 00       push   QWORD PTR [rip+0x2f8a]        # 3fb0 &lt;_GLOBAL_OFFSET_TABLE_+0x8&gt;</code></pre>
</div>
</div>
<ul>
<li><strong>Line 2</strong>: We ask <code>objdump</code> to take the file machine code <code>source/hello</code> and output assembly code <code>build/hello_2.asm</code> by disassembling it. The flag for asking <code>objdump</code> to disassemble the executable section of the file is <code>-d</code>. The flag <code>-M</code> passes options to the disassembler. Here we are asking it to use Intel syntax instead of the default AT&amp;T syntax.</li>
</ul>


</section>


<div id="quarto-appendix" class="default"><section id="footnotes" class="footnotes footnotes-end-of-document"><h2 class="anchored quarto-appendix-heading">Footnotes</h2>

<ol>
<li id="fn1"><p>For details on WSL: <a href="https://learn.microsoft.com/en-us/windows/wsl">https://learn.microsoft.com/en-us/windows/wsl</a>↩︎</p></li>
<li id="fn2"><p>For details on <code>&lt;iostream&gt;</code>: <a href="https://en.cppreference.com/w/cpp/header/iostream.html">https://en.cppreference.com/w/cpp/header/iostream.html</a>↩︎</p></li>
<li id="fn3"><p>For details on GCC: <a href="https://gcc.gnu.org/">https://gcc.gnu.org/</a>↩︎</p></li>
</ol>
</section></div> ]]></description>
  <guid>https://nuhaadhmahid.com/blog/Cpp_Crash_Course/</guid>
  <pubDate>Thu, 25 Dec 2025 18:42:17 GMT</pubDate>
</item>
</channel>
</rss>
